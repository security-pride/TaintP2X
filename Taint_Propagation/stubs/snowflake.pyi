from typing import Any, Dict, List, Optional, Union

class connector:
    class connect:
        class cursor:
            def execute(
                self,
                sql: str
            ) -> Any: ...